# The build output of cargo.
target/

# The build output of the C++ predecessor, which the working tree may still hold.
build/
dist/

# The settings product stores at run time, where it is started with the working directory as configuration directory.
genc3wb/settings.txt

# The output of a test run.
reports/

# The metadata the Finder of macOS leaves behind.
.DS_Store

# The know-how of dealing with AI agents, which is not part of product.
CLAUDE.md
